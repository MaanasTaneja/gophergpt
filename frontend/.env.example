# Set where the frontend should call the backend API. Leave empty to use relative paths (dev proxy).
# For local development (direct):
# REACT_APP_API_BASE=http://localhost:8000
# For Docker Compose deployment where backend service name is 'backend':
# REACT_APP_API_BASE=http://backend:8000
