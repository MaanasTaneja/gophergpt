# GopherGPT - PAGES Directory

This directory is for refactoring App.js into respective files and folders, this should include files that operate as the UI, such as a landing page, transition page (if this exist), and the main messaging page (chats).
