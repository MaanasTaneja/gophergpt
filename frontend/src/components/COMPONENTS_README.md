# GopherGPT - COMPONENTS Directory

This directory is for refactoring App.js into respective files and folders, this should include files that operate as individual components to support the pages, such as the main InputBox, where the user inputs their prompts, and the ChatWindow where we contain the interaction between user and agent. We'll also include the sidebar menu and history components, once the App.js has been refactored.
