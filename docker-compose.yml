version: '3.9'
services:
  backend:
    build:
      context: .
      dockerfile: backend.dockerfile
    container_name: gophergpt-webservice
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always

